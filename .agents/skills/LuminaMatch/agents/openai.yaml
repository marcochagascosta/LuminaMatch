interface:
  display_name: "LuminaMatch"
  short_description: "Repo-specific patterns and workflows for LuminaMatch"
  default_prompt: "Use the LuminaMatch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true